/** Wire format for task dates, shared by the calendar and every request. */
export const DATE_FORMAT = 'DD.MM.YYYY';

/** Mirrors MAX_TASKS_PER_DAY on the backend, which enforces the real limit. */
export const MAX_TASKS_PER_DAY = 20;
